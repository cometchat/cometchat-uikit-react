export const CometChatDividerStyle = (props) => {
  return {
    border: "none",
    width: props.style.width,
    borderTop: props.style.border,
    color: props.style.color,

    // color: props.style.color,
    // width: props.style.width,
    // border: props.style.border,
    // height: props.style.height,
    // background: props.style.background,
  };
};
