
export const timeStyle = (props) => {

    return {
        font: props.timeFont,
        color: props.timeColor,
        backgroundColor: props.backgroundColor,
        cornerRadius: props.cornerRadius,
    }
}